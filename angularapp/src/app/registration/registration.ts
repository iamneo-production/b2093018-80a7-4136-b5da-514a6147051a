export class UserLogin{
    constructor(
        email:string,
        username: string,
        userId:string,
        mobile:string,
        password:string,
        cpassword:string

    ){ }
}